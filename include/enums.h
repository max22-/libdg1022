#ifndef ENUMS_H
#define ENUMS_H

enum class WaveForm { SINUSOID, SQUARE, RAMP, PULSE, NOISE, DC, USER };
enum class Channel { CHANNEL1, CHANNEL2 };
enum class OutputState {ON, OFF};

#endif // ENUMS_H
